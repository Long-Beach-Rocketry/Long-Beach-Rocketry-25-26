syntax = "proto3";

import "nanopb.proto";

message RocketStructProto {
    string Name = 2 [(nanopb).max_length = 40];
    int32 Year = 3;
    string clubName = 4 [(nanopb).max_length = 40];
    uint64 timestamp_ms = 5;
}

message Rockets {
    repeated RocketStructProto rockets = 1;
}